# Local Models

This tracked directory is a boundary marker only. New model weights and caches belong outside the checkout under `~/models`, with `~/models/huggingface/hub` as the default Hugging Face cache.

The repository-local Slice 002 cache is immutable benchmark evidence and is not migrated by Slice 005. Model payloads, generated media, and secrets remain forbidden in Git.
